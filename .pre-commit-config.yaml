repos:
  - repo: local
    hooks:
      - id: backend-tests
        name: Backend tests
        language: system
        entry: bash -c 'cd backend && uv run pytest tests/ -q --tb=short'
        pass_filenames: false
        always_run: true
        stages: [pre-push]
